name: Look
type: module
description: Provides Look entity management for usage with Modifiers.
core_version_requirement: ^8 || ^9 || ^10
package: User interface
configure: look.settings
dependencies:
  - modifiers:modifiers (>=8.x-1.0-alpha2)
